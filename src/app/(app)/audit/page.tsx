import { ComingSoon } from "@/components/ui";

export default function AuditPage() {
  return (
    <ComingSoon
      modul={2}
      title="Audit 5R"
      desc="Penugasan, daftar Guiding Question per prinsip R, catat temuan (Low/High + foto), tandai baru/berulang, review & kirim."
    />
  );
}
