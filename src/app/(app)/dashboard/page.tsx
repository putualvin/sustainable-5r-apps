import { ComingSoon } from "@/components/ui";

export default function DashboardPage() {
  return (
    <ComingSoon
      modul={7}
      title="Dashboard"
      desc="Skor per area, tren, compliance checklist, status Red Tag, dan open follow-up (recharts)."
    />
  );
}
