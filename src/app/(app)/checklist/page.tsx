import { ComingSoon } from "@/components/ui";

export default function ChecklistPage() {
  return (
    <ComingSoon
      modul={5}
      title="Daily Checklist"
      desc="Checklist harian yes/no area-specific, reset tiap hari, empty state untuk area tanpa item, target compliance >90%."
    />
  );
}
