import { ComingSoon } from "@/components/ui";

export default function RedTagPage() {
  return (
    <ComingSoon
      modul={6}
      title="Red Tag"
      desc="Registrasi barang (foto, lokasi, tanggal), QR Code, deadline 30/90 hari, lifecycle Registered → Disposed/Returned/Relocated, reminder."
    />
  );
}
