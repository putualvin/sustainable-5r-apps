import { ComingSoon } from "@/components/ui";

export default function MasterPage() {
  return (
    <ComingSoon
      modul={9}
      title="Master Data"
      desc="CRUD area, pengguna, guiding question, dan konfigurasi item checklist per area."
    />
  );
}
